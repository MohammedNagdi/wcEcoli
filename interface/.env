# Host-side path to the wcEcoli repo root.
# The simulation worker passes this to `docker run -v` so the sim container
# can mount the model source code.  Docker requires an absolute host path.
WCECOLI_HOST_PATH=C:\Users\vmnik\OneDrive\Desktop\wcEcoli
